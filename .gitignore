node_modules/
dist/
out/
*.log

# local scratch data stores
*.sqlite
*.sqlite-shm
*.sqlite-wal
.lancedb/
.data/
.tmp/

# env / secrets (live provider keys)
.env
.env.local

# compiled Swift AX sidecar (build with: npm run build:ax)
native/ax-dump
native/ax-dump.dSYM/
native/ax-exec
native/ax-exec.dSYM/

# python bytecode from scripts/*.py (maintainer export tooling)
__pycache__/
*.pyc

# brand asset build scratch directory
app/build/*.iconset/

# electron-builder packaging output
app/dist-app/

.DS_Store
.idea/

# Claude Code local state. `.claude/skills/` IS committed — it is project tooling.
.claude/settings.local.json
.claude/worktrees/

# Generated from real recordings — carries user screen content (window titles
# have been seen to contain bearer tokens). Never committed.
test/fixtures/imagelane-queries.draft.json

# Obsidian
/docs/.obsidian/
