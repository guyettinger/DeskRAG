@app/src/renderer/src/screens/DetailView.tsx

- the size is reading 0x0 in the details
- the accessibility section extends beyond the viewport and cannot be scrolled.
- it would be nice to have the accessibility tree show hierarchy
- it would be nice to make the accessibility tree interactive
  - clicking on an accessibility tree item should show where it occurs in the keyframe
  